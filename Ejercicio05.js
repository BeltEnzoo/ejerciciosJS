//Crear una función que reciba una palabra y un número por 
//parámetro e imprima por consola  esa palabra la cantidad
//correspondiente al número indicado.

let palabra = prompt('Ingresa una palabra');
let num = prompt('Ingrese un numero');

function Ejer4(palabra, num) {
    for (let i = 0; i <= num; i ++) {
        console.log(palabra)
    }
}

Ejer4(palabra, num);

